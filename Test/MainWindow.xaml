<Window x:Class="Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Test"
        
        d:DataContext="{d:DesignInstance vm:VM_Class,IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <TabControl ItemsSource="{Binding ItemsFromModel}" DockPanel.Dock="Top">
            <!--<TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ModelHeader}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>-->
            <TabControl.Resources>
                <DataTemplate  DataType="{x:Type vm:Model}">
                    <TextBlock Text="test" Foreground="{Binding DataContext.GlobalForeColor, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}"/>
                </DataTemplate>
            </TabControl.Resources>
        </TabControl>
    </DockPanel>
</Window>
